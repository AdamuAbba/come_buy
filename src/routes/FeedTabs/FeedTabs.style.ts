import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  barStyle: {
    position: 'absolute',
    bottom: 20,
    left: 20,
    right: 20,
    borderRadius: 15,
    height: 65,
    elevation: 10,
    justifyContent: 'center',
    padding: 10,
  },
});

export {styles};
