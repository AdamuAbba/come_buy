import {StyleSheet} from 'react-native';
import {NINETY_PERCENT_WIDTH} from 'utils/constants';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    padding: 10,
    width: NINETY_PERCENT_WIDTH,
    backgroundColor: 'pink',
  },
});

export {styles};
